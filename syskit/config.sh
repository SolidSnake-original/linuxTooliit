#!/usr/bin/env bash
# syskit configuration file. Adjust defaults as needed.

# Default path to inspect for large files (can be overridden in module runtime).
SYSKIT_DEFAULT_LARGEFILE_PATH="/"

# Default minimum size (in megabytes) for the large file search module.
SYSKIT_DEFAULT_LARGEFILE_SIZE_MB=100

# Number of entries to show for process/resource listings.
SYSKIT_DEFAULT_LIST_LIMIT=10
